[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "python manage.py migrate && python manage.py init_classes && python manage.py create_president && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"